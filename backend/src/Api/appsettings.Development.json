{
  "//": "Secrets are NOT stored here. ConnectionStrings__Default and Jwt__* come from backend/.env (see .env.example), which both `dotnet run` and docker-compose read.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
